name: Upload Website

on:
  push:
    branches:
    - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v1

    - name: build
      uses: actions/checkout@v1
      run: |
          cd warehouse/web
          yarn install && yarn build
	  cd ..
	  go run ./main.go
